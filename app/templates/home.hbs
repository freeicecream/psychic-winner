{{navbar-ews}}
<div class="container">
  {{#if model.isTalentManager}}
  {{talent-report sendMsg=(action "send") model=model reports=model.talents isSent=isSent isError=isError sending=sending}}
  {{/if}}
  {{direct-report reports=model.subordinates}}
  {{#unless model.isTalentManager}}
  {{#if model.notifications.length}}
  {{warning-info report=model}}
  {{/if}}
  {{/unless}}
</div>
{{outlet}}
