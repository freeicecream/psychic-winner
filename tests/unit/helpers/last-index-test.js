
import { lastIndex } from 'ews/helpers/last-index';
import { module, test } from 'qunit';

module('Unit | Helper | last index');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = lastIndex([42]);
  assert.ok(result);
});

