
import { collectionDelimiter } from 'ews/helpers/collection-delimiter';
import { module, test } from 'qunit';

module('Unit | Helper | collection delimiter');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = collectionDelimiter([42]);
  assert.ok(result);
});

