
import { isEven } from 'ews/helpers/is-even';
import { module, test } from 'qunit';

module('Unit | Helper | is even');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = isEven([42]);
  assert.ok(result);
});

