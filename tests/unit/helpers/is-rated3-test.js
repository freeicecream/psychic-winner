
import { isRated3 } from 'ews/helpers/is-rated3';
import { module, test } from 'qunit';

module('Unit | Helper | is rated3');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = isRated3([42]);
  assert.ok(result);
});

