
import { panelOpenState } from 'ews/helpers/panel-open-state';
import { module, test } from 'qunit';

module('Unit | Helper | panel open state');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = panelOpenState([42]);
  assert.ok(result);
});

