
import { warningSubordinatesNotification } from 'ews/helpers/warning-subordinates-notification';
import { module, test } from 'qunit';

module('Unit | Helper | warning subordinates notification');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = warningSubordinatesNotification([42]);
  assert.ok(result);
});

