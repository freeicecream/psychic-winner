
import { findWarningSubordinates } from 'ews/helpers/find-warning-subordinates';
import { module, test } from 'qunit';

module('Unit | Helper | find warning subordinates');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = findWarningSubordinates([42]);
  assert.ok(result);
});

